DATABASE_URL=postgresql://app_user:app_user_dev_password@localhost:5432/clinical_signal
ANALYSIS_ENGINE_URL=http://localhost:8000
ENGINE_JWT_SECRET=dev_only_change_me_engine_jwt_secret_min_32
PHI_ENCRYPTION_KEY=dev_only_change_me_phi_crypt_key

# --- Integration health probe (GET /api/diagnostic — disable in production) ---
# DIAGNOSTIC_INTEGRATIONS_ENABLED=true

# --- Public app URL (magic links, no trailing slash) ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Production SMTP (intake magic-link emails) ---
SMTP_SERVER=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASSWORD=your_smtp_password
EMAIL_FROM_ADDRESS=intake@your-domain.com

# --- Intake module (PRD §1.3) — dev placeholders; replace in production ---
REDIS_URL=redis://localhost:6379
S3_BUCKET=dev-intake-bucket
S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=dev
AWS_SECRET_ACCESS_KEY=dev
AWS_REGION=us-east-1
# Step 2 intake chat: Amazon Bedrock Claude 3 Opus (see AWS_* above)
# Optional — legacy dashboard synthesis paths
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5
ASSEMBLYAI_API_KEY=
INTAKE_TOKEN_TTL_DAYS=7
INTAKE_TOKEN_RATE_LIMIT_PER_MIN=10
WHISPER_SERVICE_URL=http://whisper:9000
TEXTRACT_REGION=
SUPABASE_DEV_USER_ID=00000000-0000-0000-0000-000000000099
