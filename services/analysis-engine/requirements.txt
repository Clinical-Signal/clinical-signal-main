fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
PyMuPDF==1.24.10
pytesseract==0.3.13
Pillow==10.4.0
anthropic>=0.40,<1
psycopg[binary]==3.2.3
sentence-transformers==3.0.1
pgvector==0.3.6
reportlab==4.2.5
# PR5 — engine JWT auth. PyJWT verifies the HS256 token the web tier
# signs via packages/core/src/tenancy/jwt.ts. Pinned to current stable.
PyJWT==2.9.0
# SEC-11 — ClamAV INSTREAM client for /scan (clamd sidecar).
clamd==1.0.2
# Google Drive API — used by scripts/verify_drive_access.py and the
# upcoming Phase 5 Drive watcher. Pinned to the current stable lines.
google-api-python-client==2.149.0
google-auth==2.35.0
