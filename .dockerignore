node_modules/
.next/
.env
.env.local
.git/
*.md
infrastructure/
scripts/
services/
test-results/
e2e/
# packages/ and database/ are intentionally NOT excluded — the prod
# Dockerfile copies database/migrations and packages/ into the image,
# and the dev Dockerfile.web (with repo-root context) copies packages/
# alongside apps/web/ for tsconfig path resolution.
