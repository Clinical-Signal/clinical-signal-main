# @clinical-signal/shared

Shared TypeScript types, validation schemas (Zod), and constants used by both the Next.js app and any TypeScript tooling. Populated as cross-service contracts emerge in subsequent sprints.
